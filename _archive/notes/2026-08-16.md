# 2026-08-16 — 今日改動

**Live:** https://www.rowanlin1229.com  
**Repo:** `rowanlin801229/portfolio-testing01` · `master`  
**本機預覽:** `http://127.0.0.1:8766/`  
`cd "/Users/linyuxian/Desktop/portfolio testing01" && python3 -m http.server 8766 --bind 127.0.0.1`  
**Hero 實驗夾（不要當正式站）:** `/Users/linyuxian/Desktop/新作品集/portfolio hero section/` · `8765`

今天從「把喜歡的 Pandora hero 接到作品集」做到「雲的影片、簽名、行動版裁切、檔案隔離」。以下依主題，不是只抄 commit。

---

## 看哪裡

| 用途 | 路徑 |
|------|------|
| 正式作品集 | `index.html`（hero 是 `#hero.ph-hero`） |
| 上線循環影片 | `images/hero/hero-clouds.mp4`（426 正放+倒放 8 秒） |
| Kling 原檔（喜歡的那支） | `images/hero/kling/2026-08-17-hero-lower-clouds-chosen.mp4` |
| 靜態母片 / 後備 | `images/hero/hero-color-seated.jpg` |
| 簽名圖 | `images/hero/signature.png` |
| Kling 無字靜幀 | `hero-still-no-text.jpg`、`hero-still-16x9-no-text.jpg` |
| 沒有人物的原風景 | `hero-color-original.jpg`（網站不讀） |
| 過程檔 | `_archive/`（mockups / backup-2026-05 / notes / unused images） |
| 對照 | `images/hero/README.md`、`images/hero/kling/README.md`、`_archive/README.md` |

舊 hero markup 已拿掉。`index.html` 裡還留舊 CSS／JS，找不到 `.hero-desktop-shell` 就 return，不要當現況。

---

## 1. 新 hero 接到正式站

- 用一張烤進人物的風景母片，**CSS cover**，不用 Three.js / displacement mesh。
- 保留原本 nav（logo + Projects / About / Contact + EN｜中文）以及後面所有 section。
- 捲動：以人物為 transform-origin 微微推進；`overscan: 1.06`，不可以漏黑邊。
- 兩拍互斥，避免字疊在一起。
- 筆電：舊版暖黃 **radial glow** 閃爍，不是對不準的矩形。
- 平台列加 SVG icons：Mobile / Tablet / Web / Wearable / TV。
- eyebrow 黑匡背景 70%：`rgba(0,0,0,0.7)`。
- 姓名：`Yu-Hsien(Rowan) Lin`，字距最後收到 `0.03em`（中間試過 0.09）。
- 行動版曾全黑：舊 `#hero` grid + 只有 absolute 子元素把寬度收成 0；`.ph-stage` 強制 `width/min-width: 100%`，cover 不要 contain。

**Commit:** `88239f8` Replace the homepage hero with the Pandora landscape plate.

---

## 2. 文案順序（簽名「還是沒有動畫」的真正需求）

Rowan 要的不是一進頁就寫簽名，而是：

1. 先出 **Yu-Hsien(Rowan) Lin**
2. 滑動後出 **Design·Adapt·Scale**
3. 標題到位後才播 **林昱嫻** 寫入

實作：`titleProgress` 在 scroll `0.12–0.22` 淡入標題；`>= 0.98` 才 `startSigWrite()`。捲回最上面會重來。

**Commit:** `1c2767e` Sequence the hero copy: name first, then title on scroll, then signature.

---

## 3. 簽名寫入

先前用 CSS 變數 `--sig-reveal` 塞進 `clip-path`，Safari / WebKit **不會重算**，看起來像靜態圖。正式站那版就是這個，所以「還是沒有」。

改成：

- wrapper `overflow: hidden` + `#ph-sigMask` 寬度 0→100%
- 預載 `#main.visible` 之後再播（後來改成跟 scroll 標題綁）
- 約 2.6 秒，ease 2.2
- 減少動態：mask 直接 100%

**Commit:** `311b9cc` Restore the hero signature write-on on the live homepage.

Kling / clip-path 變數不要再拿回來。

---

## 4. 左邊垂直進度條

Hero 裡不出現。`#work` 進到視窗約 92% 高度才加 `.is-on`。捲回 hero 再藏。拖曳、標籤、桌面限定不變。

**Commit:** `78efdc1` Hide the homepage progress bar until Projects.

---

## 5. 雲層移動（Kling）— 結論與採用

**不要**用 3D / depth mesh 推雲（會撕人物）。

Kling 正確頁是 **Video Generation → Image to Video**，不是 Motion Control。VIDEO 3.0 常常沒有 Motion Brush，要換 2.1 / 2.5。靜幀用 `hero-still-16x9-no-text.jpg`。鎖人物／筆電／崖，只動下層雲，不要運鏡、不要瀑布動畫。

兩支：

| 檔 | 決定 |
|----|------|
| `kling_…426_0` | **喜歡，採用** |
| `kling_…439_0` | 螢幕比較有呼吸，但瀑布／天空也在動；不用。桌面已找不到 |

426 做成 **正放+倒放 8 秒** 平面 `<video>` loop（`hero-clouds.mp4`），疊在靜態 poster 上。減少動態只顯示照片。網站 CSS 螢幕光暈仍在，不靠影片裡的亮黃矩形。

原檔收到：

`images/hero/kling/2026-08-17-hero-lower-clouds-chosen.mp4`

桌面那支亂檔名已刪（內容相同）。

**Commits:** `7b8b5c8` 接循環背景 · `b1ec1d1` / `cc7c13d` 收原檔 · `2688cf7` 補靜幀。

---

## 6. 行動版 hero（只限 ≤767px）

桌面不變。

- cover `object-position: 30% 65%`，人物＋筆電螢幕要在畫面裡（直式 cover 置中會把人切掉）。
- JS `coverFocus()` 用同一個 `max-width: 767px`，光暈才對得準。
- 第二拍 `Turning complex product journeys…` **左對齊**。

檢測：https://www.rowanlin1229.com 硬重新整理。Chrome 手機模式或真機，寬度要小於 768。桌面拉寬應仍置中。

**Commits:** `7e9f3ec` · `4640cf7`

---

## 7. 檔案整理（第一步，沒改上線網址）

`ponytail-audit` 先出清單，再搬：

| `_archive/` | 內容 |
|-------------|------|
| `mockups/` | hero-mockup、design-system、三個 worklist |
| `backup-2026-05/` | 舊 backup HTML |
| `notes/` | SESSION-PROGRESS、claude.md、GROK、COLLABORATION、CURSOR-BRIEF |
| `images/` | 01.png、03.png、舊 hero／錯字簽名（`02.png` 仍在 `images/`，About 後備） |

沒搬：`uploads/`（CV 還指 `uploads/rowan-lin-cv.pdf`）、上線頁、`images/hero/`、`DESIGN.md` / `PRODUCT.md`。

下一步還沒做：改活檔檔名、依專案拆 `images/`（會動路徑）。

**Commits:** `7e9f3ec`（搬檔跟行動版同一包）· `612967c` 補 README 與未追蹤筆記。

---

## 今日 commit 一覽（`master`，已 push）

| hash | 內容 |
|------|------|
| `88239f8` | 用 Pandora 風景母片換掉首頁 hero |
| `311b9cc` | 簽名改 mask 寫入 |
| `1c2767e` | 名字 → 捲動標題 → 簽名 |
| `78efdc1` | hero 藏進度條 |
| `a189606` | 姓名字距 0.03em |
| `7b8b5c8` | Kling 426 當平面循環背景 |
| `b1ec1d1` | Kling 原檔進 `images/hero/kling/` |
| `2688cf7` | 補 original + 16:9 靜幀 |
| `cc7c13d` | 原檔改名、桌面複本刪掉 |
| `7e9f3ec` | 行動版裁切＋左對齊；開始搬 `_archive` |
| `4640cf7` | 裁切／左對齊明確只在手機 |
| `612967c` | `_archive` 剩餘 mockup／筆記 |

---

## 不要再踩

- 人物進 displacement mesh → 背部破洞、輪廓融化。重整第一秒完整再破掉 = poster 被 WebGL 換掉。
- 矩形對準整塊筆電螢幕對不準。光暈用 `imageContentRect` + radial。
- `clip-path` 裡放 CSS 變數，Safari 不更新。
- Kling 開 Motion Control（那是套人物動作）。3.0 常沒 brush。
- 雲的影片不要再丟進 mesh。
- 雙擊開 HTML 會被 CORS 擋；作品集 8766，實驗 8765。
- 改 hero 文案只動 `index.html` 的 `.ph-name` / `.ph-title`（`ph-` 前綴）。
- 行動版人物／左對齊不要套到桌面。

---

## 還沒做

- `index.html` 裡舊 hero CSS／JS（約五千行）還在，只是 return。
- Hero 文案沒接 `translations.json`（切中文不會改那兩拍）。
- Depth mesh 維持關。
- `images/` 還沒依專案拆；活檔檔名還沒清。
- React + Babel + tweaks-panel 還掛在首頁（訪客用不到）。
- Hero 中文／i18n、Kling 若要再跑：只動下層雲，瀑布和天空鎖死。

## 下次接著改 hero

只動 `index.html` 的 `#hero.ph-hero` CSS／Pandora script，以及 `images/hero/`。硬重新整理 https://www.rowanlin1229.com 或 `8766`。
