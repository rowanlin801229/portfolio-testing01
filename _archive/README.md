# _archive

不上線。從 repo 根目錄隔離出來的過程檔。  
**不要**改現在網站路徑來遷就這裡。

| 夾 | 裡面是什麼 |
|----|------------|
| `mockups/` | hero / worklist / design-system 試作頁 |
| `backup/` | 2026-05 整份 HTML 複本 |
| `notes/` | session / AI 協作筆記 |
| `images/` | 網站已不讀的舊圖（`02.png` 仍在 `images/`，About 後備圖） |

`uploads/` 沒搬：`index.html` 的 CV 還指著 `uploads/rowan-lin-cv.pdf`。
